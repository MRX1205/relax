spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/relax?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:relax}
    password: ${DB_PASSWORD:change-me}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

relax:
  auth:
    bootstrap-super-admin-open-id: ${BOOTSTRAP_SUPER_ADMIN_OPEN_ID:mock:dec1420f640fcb83a93e48d6f30ae2e5}
  file:
    local-root: ${LOCAL_FILE_ROOT:./data/private-files}
