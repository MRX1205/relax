spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:mem:relax;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  flyway:
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379
      password:

relax:
  auth:
    bootstrap-super-admin-open-id: mock:dec1420f640fcb83a93e48d6f30ae2e5
  file:
    local-root: ./data/private-files
